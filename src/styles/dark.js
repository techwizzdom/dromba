import { colors } from './colors';

export const dark = {
  isLight: false,
  isDark: true,
  backgroundColor: colors.baseBlack,
  border: colors.baseWhite,
  textColor: colors.baseWhite,
  themeToggleColor: colors.radioactiveSun,
  themeToggleLine: colors.turqoise,
};
